# 高质量数据集标注系统

## 系统架构

```
前端 (React + TypeScript + Ant Design)
  |
  v
后端 (FastAPI + Python)
  |
  ├── SQLite (数据存储，轻量易部署)
  ├── AI Engine (LLM API + Ollama 本地模型)
  └── 文件存储 (本地文件系统)
```

**项目目录结构：**
```
d:\code\data-anno\
├── backend/
│   ├── app/
│   │   ├── main.py              # FastAPI 入口
│   │   ├── config.py            # 配置管理
│   │   ├── database.py          # 数据库连接
│   │   ├── models/              # SQLAlchemy 模型
│   │   │   ├── project.py       # 项目模型
│   │   │   ├── dataset.py       # 数据集模型
│   │   │   ├── annotation.py    # 标注模型
│   │   │   └── user.py          # 用户模型
│   │   ├── schemas/             # Pydantic schemas
│   │   ├── routers/             # API 路由
│   │   │   ├── projects.py      # 项目管理
│   │   │   ├── datasets.py      # 数据集管理
│   │   │   ├── annotations.py   # 标注操作
│   │   │   └── ai.py            # AI 标注
│   │   ├── services/            # 业务逻辑
│   │   │   ├── ai_service.py    # AI 标注服务
│   │   │   └── export_service.py # 导出服务
│   │   └── utils/
│   ├── requirements.txt
│   └── Dockerfile
├── frontend/
│   ├── src/
│   │   ├── pages/               # 页面组件
│   │   ├── components/          # 通用组件
│   │   │   ├── annotation/      # 标注组件
│   │   │   │   ├── TextClassifier.tsx
│   │   │   │   ├── NERAnnotator.tsx
│   │   │   │   ├── RelationAnnotator.tsx
│   │   │   │   ├── DialogAnnotator.tsx
│   │   │   │   └── ScoreReviewer.tsx
│   │   │   └── common/
│   │   ├── services/            # API 调用
│   │   ├── stores/              # 状态管理
│   │   └── types/               # 类型定义
│   ├── package.json
│   └── Dockerfile
├── docker-compose.yml
└── .env.example
```

## 核心数据模型

- **Project**: 标注项目（名称、类型、标注配置schema、标签集）
- **Dataset**: 数据集（关联项目、数据来源、状态）
- **DataItem**: 单条数据（原始内容、元数据、状态：待标注/已标注/已审核）
- **Annotation**: 标注结果（关联数据项、标注者、标注内容JSON、版本）
- **User**: 用户（角色：管理员/标注员/审核员）

## 实施任务

### Task 1: 后端基础框架搭建
**范围**: `backend/` 目录
- 初始化 FastAPI 项目，配置 CORS、异常处理
- 配置 SQLite + SQLAlchemy（async），实现数据库迁移
- 实现 User、Project、Dataset、DataItem、Annotation 数据模型
- 实现基础 CRUD API（项目管理、数据集管理）
- 实现数据导入功能（支持 JSON、JSONL、CSV 格式上传）
- 编写 `requirements.txt`

### Task 2: 前端基础框架与页面
**范围**: `frontend/` 目录
- 用 Vite + React + TypeScript + Ant Design 初始化前端项目
- 实现整体布局（侧边栏导航 + 内容区）
- 实现项目列表页、项目创建页（选择标注类型、配置标签集）
- 实现数据集管理页（上传数据、查看数据列表、数据预览）
- 实现 API 调用服务层
- **API 契约**: 见下方 API 契约定义

### Task 3: 标注功能实现 - 后端
**范围**: `backend/app/routers/annotations.py`, `backend/app/services/`
- 实现标注任务分发 API（获取下一条待标注数据）
- 实现标注提交 API（支持多种标注类型的统一 JSON 格式）
- 实现标注审核流程 API（审核通过/驳回）
- 实现标注统计 API（进度、标注一致性）
- 依赖: Task 1 完成

### Task 4: 标注功能实现 - 前端
**范围**: `frontend/src/components/annotation/`
- 实现 TextClassifier 组件（单标签/多标签分类）
- 实现 NERAnnotator 组件（文本选中标注实体、实体类型选择）
- 实现 RelationAnnotator 组件（实体间关系连线标注）
- 实现 DialogAnnotator 组件（多轮对话展示、回复质量标注、偏好对比标注）
- 实现 ScoreReviewer 组件（多维度打分、文本评价）
- 实现标注工作台页面（整合以上组件，根据项目类型动态加载）
- 依赖: Task 2 完成

### Task 5: AI 辅助标注
**范围**: `backend/app/services/ai_service.py`, `backend/app/routers/ai.py`, 前端 AI 相关组件
- 后端：实现统一 AI 服务接口，支持 OpenAI 兼容 API 和 Ollama 本地模型
- 后端：实现批量预标注功能（异步任务处理）
- 后端：实现单条 AI 辅助标注（实时返回建议）
- 前端：在标注工作台集成 AI 辅助按钮（一键 AI 标注、采纳/修改 AI 结果）
- 前端：实现 AI 配置页面（模型选择、API Key 配置、Prompt 模板管理）
- 依赖: Task 3, Task 4 完成

### Task 6: 数据导出与 Docker 部署
**范围**: 导出服务、Docker 配置
- 实现标注结果导出（JSON、JSONL、CSV 格式）
- 支持导出为主流训练框架格式（Alpaca、ShareGPT 格式）
- 编写 `Dockerfile`（前端、后端各一个）
- 编写 `docker-compose.yml`（一键启动前后端）
- 编写 `.env.example` 配置模板

### Task 7: 集成验证
- 前后端联调测试
- Docker 构建验证
- 核心功能流程走通

## API 契约定义（Task 2 和 Task 3/4 共享）

```
# 项目管理
GET    /api/projects                    # 项目列表
POST   /api/projects                    # 创建项目 {name, type, config}
GET    /api/projects/{id}               # 项目详情

# 数据集
POST   /api/projects/{id}/datasets      # 上传数据集
GET    /api/projects/{id}/datasets      # 数据集列表
GET    /api/datasets/{id}/items         # 数据项列表

# 标注
GET    /api/datasets/{id}/next          # 获取下一条待标注
POST   /api/items/{id}/annotations      # 提交标注 {type, content}
PUT    /api/annotations/{id}/review     # 审核标注

# AI
POST   /api/ai/annotate                 # 单条 AI 标注
POST   /api/ai/batch                    # 批量预标注
GET    /api/ai/config                   # AI 配置
PUT    /api/ai/config                   # 更新 AI 配置

# 导出
GET    /api/datasets/{id}/export        # 导出标注结果

# 统计
GET    /api/projects/{id}/stats         # 项目统计
```

## 并行策略

- Task 1 (后端基础) 和 Task 2 (前端基础) **并行执行**
- Task 3 (标注后端) 依赖 Task 1，Task 4 (标注前端) 依赖 Task 2
- Task 3 和 Task 4 可**并行执行**（共享 API 契约）
- Task 5 (AI) 依赖 Task 3 + Task 4
- Task 6 (导出+部署) 依赖 Task 5
- Task 7 (验证) 最后执行
